# 🎨 Coding Standards

- **Language:** TypeScript strictly enforced.
- **UI:** CSS styles must map variables from [globals.css](file:///e:/K4NN4N/Threadly/apps/web/app/globals.css).
- **Workspaces:** Frontend packages live in `/apps/web`.
